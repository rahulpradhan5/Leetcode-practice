/**
 * @param {string} s
 * @return {boolean}
 **/
s = "abab";
var repeatedSubstringPattern = function (s) {
    var map = new Map();
    var arr = []
    s = [...s];
    if (map.has(s[i])) {
        
    } else {
        map.set(s[i]);
    }
};
console.log(repeatedSubstringPattern(s));
